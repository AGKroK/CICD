name: Hellow World
on: [push] #событие 
jobs: #работы
  hello: #название работы
    runs-on: ubuntu-latest #OS
    steps: #шаги
      - name: Hello  #название шага
        run: echo "Hellow World" #действия
      - name: Check out repository code
        uses: actions/checkout@v2
      - name: Run Tests
        run: python3 test.py      
        